# Runtime dependency for externalscripts/mail.dns.audit
#
# Ставится не в системный Python, а в отдельное окружение по пути, на который
# указывает шебанг скрипта. Одинаково на сервере Zabbix и на каждом прокси:
#
#   mkdir -p /opt/zabbix-mail-dns
#   /usr/bin/python3.12 -m venv /opt/zabbix-mail-dns/venv
#   /opt/zabbix-mail-dns/venv/bin/pip install -r requirements.txt
#   chown -R zabbix:zabbix /opt/zabbix-mail-dns
#
# Проверка после установки:  mail.dns.audit --selfcheck
dnspython>=2.6
